.benefit{
    display: flex;
    flex-wrap: wrap;
    margin-bottom: 15.7em;
    padding-left: 1.5em;
   .caption{
       width: 100%;
       margin-bottom: 5em;
       margin-top: 3.6em;
       span{
            color: #FFFFFF;
            font-size: 2.4em;
       }
       &.trader{
           margin-bottom: 6em
       }
   }
   .column-half{
       width: 50%;
       position: relative;
   }
   .img-wrapper{
       
       position: absolute;
       img{
           width: 100%;
       }
   }
   .left-half{
       .img-wrapper{
            bottom: 0em;
            max-width: 6em;
       }
   }
   .right-half{
        padding-top: 9.2em;
        margin-bottom: 4.1em;
        .img-wrapper{
            max-width: 8.2em;
            top: -4.1em;
            right: 1em;
        }
        .benefit-item{
            margin-left: auto;
        }
        .img-wrap{
            max-width: 10em;
            margin: -1.5em auto 0;
            img{
                width: 100%;
            }
        }
    }
   .benefit-item{
        width: 87%;
        padding: 2.2em 1em 3.5em 1.5em;
        background: rgba(244, 244, 244, 0.11);
        
        p{
            color: #FFFFFF;
            font-size: .9em;
            margin-bottom: 1em;
            letter-spacing: 1.1px;
            line-height: 1.3;
            margin-bottom: 1.6em;
            font-weight: bold;
        }
        .small-text{
            font-size: .7em;
            margin: 0;
            line-height: 1.75;
            letter-spacing: 1px;
            font-weight: 200;
        }
   }
   .img-wrap{
       text-align: center;
       margin-bottom: 2.4em;
   }
   .girl-wrap{
       p{
           margin-top: .5em;
           letter-spacing: 0.25px;
            margin-bottom: 1.85em;
       }
   }
   .item-3{
      .benefit-item{
        padding-left: 2em;
      }
       .img-wrap{
            margin-bottom: 1.8em;
       }
       p{
           letter-spacing: .5px
       }
   }
   .item-4{
        padding-top: 8.2em;
        .benefit-item{
            padding-left: 2em;
        }
        .img-wrap{
            margin-bottom: 1.4em;
            margin-top: 0;
            max-width: 7.2em;
        }
        p{
            letter-spacing: .5px
        }
    }
}